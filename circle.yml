---
machine:
  ruby:
    version: 2.2.3
  node:
    version: 5.0.0
  environment:
    NOKOGIRI_USE_SYSTEM_LIBRARIES: true # speeds up installation of html-proofer
compile:
  override:
    - bundle exec jekyll build
test:
  override:
    - "true"
general:
  artifacts:
    - "_site"
